{
  "app": {
    "name": "Edmunds Instance"
  },

  "filesystem": {
    "instances": [
      {
        "name": "local",
        "driver": "local",
        "path": "./storage/files"
      }
    ]
  },

  "logging": {
    "instances": [
      {
        "name": "console",
        "driver": "Console"
      },
      {
        "name": "file",
        "driver": "File",
        "filename": "./storage/logs/error-log.txt",
        "level": "error"
      }
    ]
  },

  "cache": {
    "instances": [
      {
        "name": "memcached",
        "driver": "memcached"
      }
    ]
  }
}
